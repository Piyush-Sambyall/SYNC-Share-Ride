# Copy this file to .env and fill in your own values.

# Where the Express/Socket.io server (in /server) is running
VITE_API_BASE_URL=http://localhost:4000

# No Google Maps key needed anymore — the app uses free OpenStreetMap tiles
# and the free OSRM routing service instead, so there's nothing to sign up
# for here.
